# Tabelogo v2 - Environment Configuration Template
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# Google Maps API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Database Passwords (optional - defaults are set in docker-compose.yml)
# POSTGRES_PASSWORD=your_secure_password_here

# JWT Secrets (optional - defaults are set in docker-compose.yml)
# JWT_SECRET=your_secure_jwt_secret_here_at_least_32_characters_long

# Redis Password (optional)
# REDIS_PASSWORD=your_redis_password_here
